# Version 0.9.8_beta - AFK Monitor, Automated Deck Removal, Moderator Commands & More

**Release Date:** 2026-03-14  
**Type:** Feature Release (Beta)

## Contents

- [AFK Monitor command (`!afkMonitor`)](#afkmonitor-command)
- [Automated AFK deck removal](#automated-afk-deck-removal)
- [Moderator commands (`!mod`)](#moderator-commands)
- [Session Theme Tokens (`{theme}` / `{readTheme}`)](#session-theme-tokens)
- [Per-User Personalised Welcome Messages (`!editWelcome`)](#per-user-personalised-welcome-messages)

---

## ✨ New Features & Improvements

### `!afkMonitor` Command

A new system admin command for managing the AFK monitor at runtime. Requires the **afkMonitor** feature to be enabled (`!feature enable afkMonitor`).

#### Subcommands

| Subcommand | Example | Role | Description |
|---|---|---|---|
| `status` | `!afkMonitor status` | MODERATOR | Show all current DJs, their last-activity age, per-type breakdown, and current thresholds |
| `exempt <name>` | `!afkMonitor exempt "DJ Cool"` | OWNER | Exclude a DJ from AFK warnings for the current session |
| `reset <name>` | `!afkMonitor reset "DJ Cool"` | OWNER | Manually reset a DJ's activity timer to now |
| `set warning <sec>` | `!afkMonitor set warning 900` | OWNER | Set the inactivity period before the first warning (in seconds) |
| `set interval <sec>` | `!afkMonitor set interval 60` | OWNER | Set the time between subsequent warnings (in seconds) |

#### `status` output

Lists every DJ currently on the decks with:
- **Last active** — human-readable age of the most recent activity (`just now` / `Xm ago`)
- **Per-type ages** — ages for `chat`, `emoji`, `vote`, `queue`, `decks`, and `room` signals — shown as `—` when that type has never fired
- **Exempt tag** — `(exempt)` appended if the DJ has been exempted from AFK monitoring this session
- **Thresholds** — current `firstWarning` and `interval` values shown at the bottom

#### Exemptions

Exempting a DJ (`!afkMonitor exempt <name>`) suppresses all AFK warnings for that person until:
- Their session ends (they leave the decks or the room), or
- A moderator manually resets them (`!afkMonitor reset <name>`), which clears the exemption and restarts their activity timer from now

#### Name matching

- Exact case-insensitive match is tried first
- Falls back to a partial (contains) match if no exact match is found
- Wrap names with spaces in quotes: `!afkMonitor exempt "DJ Cool"`

#### Example workflow

```
// Check current activity
!afkMonitor status

// Guest DJ is known to be AFK — exempt them for the rest of their set
!afkMonitor exempt "DJ Vinyl"

// They're back — clear the exemption and reset their timer
!afkMonitor reset "DJ Vinyl"

// Adjust the warning window to 20 minutes
!afkMonitor set warning 1200

// Tighten the interval between warnings to 30 seconds
!afkMonitor set interval 30
```

---

### Automated AFK Deck Removal

After the third AFK warning, if a DJ remains inactive for one further `interval` period, the bot automatically removes them from the decks.

The removal threshold is therefore: `firstWarning + 3 × interval`.

#### Position-aware removal

Removal behaviour depends on whether the DJ is currently at deck position 0 (i.e. actively playing):

- **Not currently playing** — the DJ is removed immediately via a `removeDj` socket action and a public confirmation message is posted.
- **Currently playing (position 0)** — the removal is deferred. The bot posts a public notice that the DJ will be removed after their current song ends. When the next song starts, the pending removal is executed automatically.

If the DJ shows any activity before the next song ends, the pending removal is cancelled.

Once removed (or marked for deferred removal), the DJ's warning level is recorded as `4` to prevent repeated removal attempts. The existing `!afkMonitor exempt` command can be used to prevent any DJ from reaching the removal stage.

#### Queue activity detection

Manually changing a queued song counts as activity and resets the AFK timer. Automatic song advances (where the platform replaces the whole `nextSong` object at the end of a track) are explicitly ignored and do not reset the timer.

---

### Moderator Commands

A command group for anonymous moderator actions (requires **MODERATOR** role). Responses are sent to the same channel the command was issued from — privately if the command was sent via private message, publicly if sent in the hangout chat.

| Subcommand | Example | Description |
|---|---|---|
| `listUsers` | `!mod listUsers` | List all users currently in the hangout, sorted alphabetically and wrapped in double quotes |
| `remove <name>` | `!mod remove "DJ Cool"` | Remove the named DJ from the decks |
| `skip` | `!mod skip` | Skip the currently playing song |

- Names are matched case-insensitively
- Names containing spaces work with or without surrounding double quotes
- `remove` returns an error if the user is not found or is not currently on the decks
- `skip` uses the `skipSong` socket action; the bot must have sufficient room permissions for this to succeed

---


### Session Theme Tokens

#### `{theme}` Token
- **Editable message**: Set with `!edit theme <value>` (requires OWNER role)
- **Persists across restarts**: Stored in `data/botConfig.json` via DataService
- **Cleared cleanly**: Use `!edit clear theme` to remove the theme — the token resolves to an empty string when unset
- **Available anywhere**: Can be used in any message template that supports tokens (welcome message, now playing, etc.)

#### `{readTheme}` Token
- **Smart self-suppression**: Returns an empty string when no theme is set, so surrounding message text doesn't look odd
- **Editable template**: The announcement phrasing is itself configurable — set with `!edit readTheme <template>`, e.g. `!edit readTheme Tonight's theme is {theme}`
- **Default template**: If `readTheme` has never been edited, the default is `The theme is currently {theme}`
- **Persists across restarts**: Template stored in `data/botConfig.json` via DataService
- **Recommended usage**: Use `{readTheme}` in your welcome message rather than hardcoding the announcement text — it will automatically disappear when no theme is active

#### Example Workflow

```
// Set a theme for the night
!edit theme 90s house music

// Welcome message containing {readTheme} now produces:
// "Good evening Username and welcome to Hangout FM. The theme is currently 90s house music"

// At the end of the night, clear the theme
!edit clear theme

// Welcome message now produces (readTheme returns empty string):
// "Good evening Username and welcome to Hangout FM. "

// Customise the readTheme announcement phrasing
!edit readTheme Tonight's theme is {theme}
// Now produces: "Tonight's theme is 90s house music"
```

### `!edit clear` Subcommand

- **New subcommand**: `!edit clear <messageType>` unsets any editable message, restoring it to its default or empty value
- **Usage**: `!edit clear theme`, `!edit clear welcomeMessage`, `!edit clear readTheme`, etc.
- **Requires OWNER role**
- **Updated help text**: `!edit` usage now shows `clear` alongside `list`, `show`, and the update syntax

---

### Per-User Personalised Welcome Messages

Each DJ can now have their own custom welcome message(s) and image(s) that are served at random when they join the hangout. If no personalised data exists for a joining user, the standard `welcomeMessage` template is used as a fallback.

#### How it works

- Personalised data is stored in `data/welcomeMessages.json`, keyed by user UUID
- Each entry holds a `messages` array and a `pictures` array — the same structure used by chat commands
- When a user joins, one message is picked at random; if pictures are present, one is also picked at random
- The `{username}` token is available inside personalised messages and is replaced with a mention of the arriving user
- When a picture is selected, it is sent as an image message (same as `sendGroupPictureMessage`); when there is no picture, it is sent as a plain text message

#### `!editWelcome` Command

Manages per-user personalised welcome data. Requires **OWNER** role.

| Subcommand | Example | Description |
|---|---|---|
| `list` | `!editWelcome list` | List all users who have custom welcome data, with message and image counts |
| `show` | `!editWelcome show "Happy Gilmore"` | Show all messages and images stored for a user |
| `addMessage` | `!editWelcome addMessage "Happy Gilmore" Hey {username}!` | Add a welcome message for a user |
| `removeMessage` | `!editWelcome removeMessage "Happy Gilmore" Hey {username}!` | Remove an exact-match message |
| `addImage` | `!editWelcome addImage "Happy Gilmore" https://media.giphy.com/img.gif` | Add a welcome image URL |
| `removeImage` | `!editWelcome removeImage "Happy Gilmore" https://media.giphy.com/img.gif` | Remove an exact-match image URL |
| `remove` | `!editWelcome remove "Happy Gilmore"` | Remove all custom welcome data for a user |

#### Nickname handling

- User identifiers can be a **UUID** or a **nickname**
- Nicknames containing spaces must be wrapped in **double quotes**: `"Happy Gilmore"`
- Single-word nicknames work without quotes: `!editWelcome show JohnDoe`
- Lookups are **case-insensitive** and tolerate extra whitespace (e.g. double spaces) in stored names
- If a nickname is not found, the bot performs a **fuzzy match** across live state and the database and returns up to 3 suggestions, each quoted so they can be copy-pasted directly

#### Example workflow

```
// Add a couple of messages and an image for a user
!editWelcome addMessage "Happy Gilmore" Prepare your facial hair {username} — the surgery is open!
!editWelcome addMessage "Happy Gilmore" Welcome back {username}, good to see you!
!editWelcome addImage "Happy Gilmore" https://c.tenor.com/qosLRuL-U1oAAAAC/69.gif

// Check what's stored
!editWelcome show "Happy Gilmore"

// Remove one message
!editWelcome removeMessage "Happy Gilmore" Welcome back {username}, good to see you!

// Remove all data for the user
!editWelcome remove "Happy Gilmore"
```

#### Tokens available in personalised messages

| Token | Replaced with |
|---|---|
| `{username}` | A @mention of the arriving user |

> Other token service tokens (e.g. `{hangoutName}`, `{theme}`) are also resolved if the token service is available.
